pnpm lint-staged
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --no-banner
else
  echo "warning: gitleaks not installed locally; the CI secret scan remains authoritative (SECURITY.md)"
fi
